- name: Prune docker
  hosts: all
  tags: docker
  tasks:
    - name: prune docker
      cron:
        name: "Docker system prune"
        special_time: "weekly"
        job: /usr/local/bin/docker system prune -f
