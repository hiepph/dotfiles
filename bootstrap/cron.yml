- name: Prune docker
  hosts: all
  tags: docker
  tasks:
    - name: prune docker
      cron:
        name: "Docker system prune"
        special_time: "daily"
        job: /usr/local/bin/docker system prune -f
