#!/usr/bin/env ruby
require 'date'

date = Date.today

last_sunday = date-date.wday
last_monday = last_sunday - 6
last_friday = last_monday + 4
format = "%b %d"
puts "#{last_monday.strftime(format)} - #{last_friday.strftime(format)}"
